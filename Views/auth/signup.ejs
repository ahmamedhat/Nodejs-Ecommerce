<%- include ('../includes/header.ejs') %>
    <body class="login__body">
        <%- include ('../includes/navigation.ejs') %>

        <main>
            <div class="login__container signup__container">
                <% if (errorMessage) { %>
                    <div class="popup-message popup-message--error">
                        <%= errorMessage %>
                    </div>
                <% } %> 
                <div class="login__container--form signup__container--form">
                    <form action="/signup" method="POST">
                        <div class="login__container--field">
                            <input class="form__field <%= errorMessages.find(e => e.param === 'email') ? 'invalid' : ''%>" type="text" placeholder="Email" name="email" value="<%= oldInput.email %>">
                            <label class="form__label" for="email"><b>Email</b></label>
                        </div>
                        <div class="login__container--field">
                            <input class="form__field <%= errorMessages.find(e => e.param === 'password') ? 'invalid' : ''%>" type="password" placeholder="Password" name="password" value="<%= oldInput.password %>">
                            <label class="form__label" for="password"><b>Password</b></label>
                        </div>
                        <div class="login__container--field">
                            <input class="form__field <%= errorMessages.find(e => e.param === 'confirmPassword') ? 'invalid' : ''%>" type="password" placeholder="Comfirm Password" name="confirmPassword" value="<%= oldInput.confirmPassword %>">
                            <label class="form__label" for="password"><b>Confirm Password</b></label>
                        </div>
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <button class="login__container--submit signup__container--submit" type="submit">Signup</button>
                    </form>
                </div>
            </div>
        </main>
    </body>
</html>